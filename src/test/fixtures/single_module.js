import { module, test } from 'qunit';
import { setupApplicationTest } from 'ember-qunit';

// moduleId: cca93cea
module('module example', function(hooks) {
  setupApplicationTest(hooks);

  test('test example 1', async function(assert) {
    assert.ok(1 == 1);
  });
});
